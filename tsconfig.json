/>

1. Remova a chave “paths” inteira (a que redirecionava next-intl/navigation).
2. Remova também qualquer referência a “lib/intl-navigation.ts”.

<QuickEdit file="lib/intl-navigation.ts" />

• Excluir completamente este arquivo (não é mais necessário).

<QuickEdit file="middleware.ts" />

1. Substitua todo o conteúdo por:
```ts
import createMiddleware from "next-intl/middleware"
import { LOCALES } from """"""""""""""""""""""""""""""""""""""""""""""""""""""@"/"l"i"b"/"i"18n"

export default createMiddleware({
  locales: LOCALES as unknown as string[],
  defaultLocale: "en",
  localePrefix: "as-needed",
})
